# Raiz / CI — copie para `.env` local conforme o app que estiver a correr.

# --- API (apps/api) ---
NODE_ENV=development
# api = HTTP Nest (predefinido) | worker = apenas consumidores BullMQ
PROCESS_ROLE=api
DATABASE_URL="postgresql://navomnis:navomnis@localhost:5432/navomnis?schema=public"
REDIS_URL="redis://localhost:6379"
JWT_ACCESS_SECRET="change-me-access-min-32-chars-long-secret"
JWT_REFRESH_SECRET="change-me-refresh-min-32-chars-long-secret"
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES="7d"
API_PORT=4000
# Origens CORS tenant ERP (CSV). Em produção deve listar URLs reais do front web.
WEB_URL="http://localhost:5173"
# Origens CORS platform admin (CSV). Ex.: http://localhost:5174 ou https://navomnis-admin-oss365.vercel.app
ADMIN_WEB_URL="http://localhost:5174"
# JWT platform (distintos dos tenant; mín. 32 chars em NODE_ENV=production)
PLATFORM_JWT_ACCESS_SECRET="change-me-platform-access-min-32-chars"
PLATFORM_JWT_REFRESH_SECRET="change-me-platform-refresh-min-32-chars"
PLATFORM_JWT_ACCESS_EXPIRES="15m"
PLATFORM_JWT_REFRESH_EXPIRES="7d"
# Em produção: omitir ou false. Só true em ambientes privados/staging com credenciais abaixo.
SWAGGER_ENABLED=""
# Obrigatório em produção quando SWAGGER_ENABLED=true (Basic Auth em /api/docs).
SWAGGER_BASIC_USER=""
SWAGGER_BASIC_PASSWORD=""
RESEND_API_KEY=""
SENTRY_DSN_API=""
# --- Open Finance Brasil (apps/api) ---
# false até prova V1 (docs/open-finance/v1-gate.md)
OPEN_FINANCE_ENABLED=false
ENCRYPTION_KEY=""
OF_DIRECTORY_URL=""
OF_CLIENT_ID=""
OF_REDIRECT_URI="http://localhost:4000/api/v1/banking/oauth/callback"
# MTLS_CERT="" MTLS_KEY="" (produção/homologação)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
APPLE_CLIENT_ID=""
APPLE_TEAM_ID=""
APPLE_KEY_ID=""

# --- Web (apps/web / Vite) ---
# Em build de produção (Vercel) é obrigatório definir a URL pública da API (ex.: https://api.seudominio.com/api/v1).
VITE_API_URL="http://localhost:4000/api/v1"
VITE_APP_ENV=development
VITE_PROXY_API="http://localhost:4000"
VITE_SENTRY_DSN=""
VITE_PUSH_PUBLIC_KEY=""
VITE_GOOGLE_CLIENT_ID=""
VITE_APPLE_CLIENT_ID=""

# --- Admin (apps/admin / Vite) ---
VITE_API_URL="http://localhost:4000/api/v1"
VITE_APP_ENV=development

# --- Vercel (local / CI; não commitar tokens) ---
# VERCEL_ORG_ID=team_VTLR2O8dPIC8E0ogQi93uCk9
# VERCEL_PROJECT_ID=prj_lB9fMjqKg7uj2CMaSh0slXuUR3XP
# VERCEL_ADMIN_PROJECT_ID=prj_tCK7lc0TV4fHOMnK4tpfkSrQD4Ax
# VERCEL_TOKEN=

# --- Railway (local / CI; não commitar tokens) ---
# RAILWAY_TOKEN=

# --- CADEG legacy import (apps/api provision:cadeg) ---
# CADEG_DATA_DIR="C:/path/to/CADEG DATA BASE"
